<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PetCompanyApp">

        <activity
            android:name=".activities.PostCreateActivity"
            android:exported="false" />
        <activity
            android:name=".activities.FeedActivity"
            android:exported="false" />
        <activity
            android:name=".activities.AnimalRegisterActivity"
            android:exported="false" />
        <activity
            android:name=".activities.CompanyRegisterActivity"
            android:exported="false" />
        <activity
            android:name=".activities.UserRegisterActivity"
            android:exported="false" />
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
